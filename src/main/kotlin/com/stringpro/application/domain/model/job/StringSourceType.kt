package com.stringpro.application.domain.model.job

/**
 * Where a single side's string comes from: the Customer's Own String, or one of the Stringer's Reels.
 */
enum class StringSourceType {
    OWN,
    REEL,
}
